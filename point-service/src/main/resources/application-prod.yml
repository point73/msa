server:
  port: 8080

spring:
  datasource:
    url: ${POINT_DB_URL}
    username: ${POINT_DB_USERNAME}
    password: ${POINT_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false